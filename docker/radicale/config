[server]
hosts = 0.0.0.0:5232

[auth]
type = htpasswd
htpasswd_filename = /config/htpasswd
htpasswd_encryption = bcrypt

[storage]
filesystem_folder = /data

[rights]
type = owner_only

[logging]
level = info
