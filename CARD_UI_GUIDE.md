# Moved

Card design guidance now lives in [`UI_CONSISTENCY_GUIDE.md`](UI_CONSISTENCY_GUIDE.md)
§1 — folded into the general app-wide UI consistency guide (buttons, forms,
tables, modals, tags, toolbars, empty states, icons) rather than kept as a
cards-only document. Read that file instead; this stub is kept only so
existing links/bookmarks don't 404.
