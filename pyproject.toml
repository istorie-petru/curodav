[project]
name = "command-center"
version = "2.0.0"
description = "Self-hosted PWA -- calendar, tasks, contacts, projects, synced via CalDAV/CardDAV"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["webapp"]
