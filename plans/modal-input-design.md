# Design: 5 reusable data-entry patterns

**Status:** ✅ Done (2026-08-07) — all 5 phases implemented and verified; full suite green at 593 passed. The 5 reusable input patterns (tile/card picker, segmented control, swatch-grid popover, chip multiselect, stepper) are now the standard form vocabulary across the webapp; see the modal/input components in `webapp/src/static/style.css` and `webapp/src/templates/` (`_modal_footer.html`, `_widget_list_multiselect.html`, `_widget_builder_fields.html`). Outcome is folded into `webapp/` directly; no further action from this plan.
